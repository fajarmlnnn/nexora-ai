# Auth Bridge Note

The server verifies Supabase access tokens through the Auth API. This is intentionally compatible with Supabase signing-key configurations without implementing JWT parsing or cryptography in application code.
