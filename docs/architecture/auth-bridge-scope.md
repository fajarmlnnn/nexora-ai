# Auth Bridge Scope

This PR is intentionally server-only. It establishes a verified Supabase identity boundary for the Laravel AI gateway. Flutter authentication remains unchanged until a separate client integration is tested.
