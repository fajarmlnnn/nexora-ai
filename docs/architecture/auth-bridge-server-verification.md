# Auth Bridge Verification

The server uses Supabase Auth verification rather than implementing JWT cryptography in application code. This keeps the verification contract aligned with the existing Supabase identity provider.
